import { FilterReclamoPipe } from './filter-reclamo.pipe';

describe('FilterReclamoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReclamoPipe();
    expect(pipe).toBeTruthy();
  });
});
